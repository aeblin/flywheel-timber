<section class="js-height-sync js-fadeIn product-image product-image-carousel" data-fullscreen="false">
  {% for image in product.images %}
    <div class="product-slide">
      <img src="{{ image.src | product_img_url: 'large' }}" data-image="{{ image.src | product_img_url: 'large' }}">
    </div>
  {% endfor %}
</section>